name: Test ASTA Luxembourg WDAA browser

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: asta-luxembourg-wdaa-browser-probe
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v6

      - name: Python einrichten
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Playwright installieren
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install playwright
          python -m playwright install --with-deps chromium

      - name: Offline-Selbsttest
        shell: bash
        run: |
          set -euo pipefail
          python scripts/test_asta_luxembourg_wdaa_browser.py --self-test

      - name: ASTA WDAA-Client mit Chromium untersuchen
        shell: bash
        run: |
          set -euo pipefail
          python scripts/test_asta_luxembourg_wdaa_browser.py \
            --wait-seconds 6
